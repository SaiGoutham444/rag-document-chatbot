# ─────────────────────────────────────────────────────────────────
# TESTING FRAMEWORK
# ─────────────────────────────────────────────────────────────────

# pytest — the standard Python testing framework.
# Discovers test files automatically (anything named test_*.py).
# Runs all functions named test_*() and reports passes/failures.
pytest==8.3.2

# pytest-asyncio — enables testing of async functions.
# Some LangChain operations are async; this plugin handles them.
pytest-asyncio==0.23.8

# pytest-cov — measures code coverage.
# Reports: which lines of src/ were executed during tests.
# Goal: >80% coverage means most code paths are tested.
pytest-cov==5.0.0

# ─────────────────────────────────────────────────────────────────
# CODE QUALITY
# ─────────────────────────────────────────────────────────────────

# black — "the uncompromising code formatter".
# Automatically reformats Python code to a consistent style.
# You never argue about formatting again — black decides everything.
# Run: black src/ tests/
black==24.8.0

# ruff — extremely fast Python linter (written in Rust, 10-100x faster than flake8).
# Catches: unused imports, undefined variables, style violations, common bugs.
# Replaces: flake8, isort, pyupgrade, and many other linting tools in one.
# Run: ruff check src/ tests/
ruff==0.5.7

# httpx — modern HTTP client for Python.
# Required for testing any code that makes HTTP requests (e.g., mocking API calls).
httpx==0.27.0